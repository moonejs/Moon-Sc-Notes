![[Pasted image 20260121132737.png]]


#### `date`

---

#### `cal`

---
#### `free`

command to check the specs or memory of the computer or machine

`free -h`

human readable 

---
#### `groups`

## What is a group in Linux? 

In Linux:

- **Users** belong to one or more **groups**
    
- Groups are used to **control permissions** (files, folders, commands)
    

Example:

- A file may allow access only to users in the `sudo` or `docker` group.

```bash
groups
```

moon sudo users

- Being in `sudo` means you can run admin commands

---

